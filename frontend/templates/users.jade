a.button.margin-bottom.indented(href='/' data-hook='async-link') &lt; back

h1.indented Manage Users

ul.admin-list
    li.admin-list-title.admin-list-item
        span.quarter name
        span.half email
        span.quarter created
    each user in data
        li.admin-list-item
            a(href='/users/#{user._id}' data-hook='async-link')
                span.quarter= user.name
                span.half= user.email
                span.quarter= moment(user.createdAt).format('D.M.YYYY HH:mm:ss')

a.indented.button(href='/users/add' data-hook='async-link') Create new user
